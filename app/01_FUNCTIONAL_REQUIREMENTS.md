# 📋 Functional Requirements
## SmartTour360 Android App
**Document ID:** STS-FR-001 | **Version:** 2.0 | **Patent Aligned:** LPU IDF 2026
**Team:** Manas Maheshwari (12218829) · K. Venkata Ram (12219197)

---

## FR-01 · User Authentication & Profile

| ID | Requirement |
|---|---|
| FR-01.1 | The app shall allow users to register via email/phone + OTP using Firebase Auth |
| FR-01.2 | The app shall support Google Sign-In via Firebase Auth |
| FR-01.3 | On first launch, users shall complete an onboarding preferences screen: budget level, trip type, eco-priority toggle |
| FR-01.4 | User identity shall be **pseudonymized** (HMAC-SHA256) before any data is sent to the backend — no PII transmitted in plaintext |
| FR-01.5 | The app shall persist auth tokens in `EncryptedSharedPreferences` / Jetpack DataStore |
| FR-01.6 | Users shall be able to view and edit their profile, saved trips, and past bookings from the Profile screen |

---

## FR-02 · Home Screen & Discovery

| ID | Requirement |
|---|---|
| FR-02.1 | The home screen shall display a curated list of top-rated destinations each with its Safety Flag chip (Green / Yellow / Red) and Eco Score |
| FR-02.2 | The home screen shall show featured hotels with eco-badge, star rating, and price-per-night |
| FR-02.3 | The app shall display horizontal category chips: Destinations · Hotels · Packages · Trending |
| FR-02.4 | A personalized "Recommended For You" section shall render destinations ranked by the backend Recommendation Engine based on user onboarding preferences |
| FR-02.5 | Pull-to-refresh shall trigger a fresh `/safety/evaluate` + `/recommendations/rank` call for updated data |

---

## FR-03 · Search & Suggestions

| ID | Requirement |
|---|---|
| FR-03.1 | The search bar shall issue queries to the backend with 300ms debounce using Kotlin `Flow` |
| FR-03.2 | Search results shall include destinations, hotels, and packages — each displaying safety flag, eco score, and rating |
| FR-03.3 | Recent searches shall be persisted in Room DB locally |
| FR-03.4 | Trending destinations shall be fetched from the backend recommendations endpoint |

---

## FR-04 · Safety Flag System (Core Patent Feature)

| ID | Requirement |
|---|---|
| FR-04.1 | The app shall call `POST /api/v1/safety/evaluate` with destination `location_id`, `latitude`, and `longitude` for every destination shown |
| FR-04.2 | The response `flag` field shall be visually rendered as: 🟢 GREEN / 🟡 YELLOW / 🔴 RED on all cards and detail screens |
| FR-04.3 | Flag thresholds shall reflect the backend model: GREEN = risk < 0.35, YELLOW = 0.35–0.70, RED ≥ 0.70 |
| FR-04.4 | Each destination detail screen shall display a **component breakdown bar** showing structural (crime), situational (weather), and environmental (AQI) sub-scores |
| FR-04.5 | A **"Why this flag?"** button shall open a BottomSheet that displays the `explanation` field from the API — the plain-language XAI output generated by SHAP + NL templates on the backend |
| FR-04.6 | The XAI explanation shall always name the dominant risk factor (e.g., *"Primary driver: Level 4 State Dept Advisory — Do Not Travel. Secondary: AQI 187."*) |
| FR-04.7 | For RED-flagged destinations, the app shall display the `alternatives` array from the API as "Safer Options" cards |
| FR-04.8 | GPS-based safety lookup: the app shall use `FusedLocationProviderClient` to obtain current lat/lon and automatically evaluate the user's current destination on the home screen |

---

## FR-05 · Blockchain Verification (Patent Feature)

| ID | Requirement |
|---|---|
| FR-05.1 | Every safety evaluation response includes a `blockchain_ref` (transaction hash). This shall be displayed as a **"Verified ✓"** chip on destination cards and detail pages |
| FR-05.2 | Tapping the "Verified ✓" chip shall call `GET /api/v1/safety/verify/{location_id}` and display the result: `verified: true/false` + the on-chain hash |
| FR-05.3 | When a user attempts to book a RED-flagged destination, the app shall show an **acknowledgement dialog**: *"This destination is flagged RED. I understand the risk and choose to proceed."* |
| FR-05.4 | On acknowledgement confirmation, the app shall call `POST /api/v1/booking/acknowledge` which logs the user's consent to the blockchain via the `UserAcknowledgement` Solidity contract |
| FR-05.5 | The acknowledgement `booking_id` and `flag_at_booking_time` shall be saved locally in Room DB for user reference in "My Bookings" |

---

## FR-06 · IoT Real-Time Data Display (Patent Feature)

| ID | Requirement |
|---|---|
| FR-06.1 | The destination detail screen shall display **live IoT-sourced metrics**: current temperature, AQI (Air Quality Index), crowd density level, and wind speed — all sourced from the backend's Kafka + InfluxDB pipeline |
| FR-06.2 | The app shall display a **data freshness indicator**: time since last IoT reading (e.g., "Updated 2 min ago") — derived from the `timestamp` field in the API response |
| FR-06.3 | If IoT data is older than 15 minutes (backend `DATA_STALENESS_01` guardrail), the app shall show a warning: "⚠️ Live data unavailable — showing last known values" |
| FR-06.4 | The IoT metrics shall visually change when a WebSocket alert updates the destination's flag in real time (see FR-10) |

---

## FR-07 · Eco Score & Responsible Tourism (Patent Feature)

| ID | Requirement |
|---|---|
| FR-07.1 | Every destination and hotel shall display an **Eco Score (0–100)** sourced from the backend recommendation engine (GreenCert API + carbon footprint model) |
| FR-07.2 | Destinations shall display a **carbon footprint estimate** (kg CO₂) for traveling to and staying there |
| FR-07.3 | Hotels with `eco_certified: true` shall show a 🌿 eco badge prominently on cards |
| FR-07.4 | The Recommendation screen shall rank destinations by the backend's **Sustainability Index** — showing which option has the lowest eco-impact |
| FR-07.5 | The app shall display an **Ethical Score chip** (HIGH / MODERATE / LOW) derived from the backend IF-THEN patent rule: IF eco-friendly AND weather safe AND fair pricing THEN Ethical Score = HIGH |
| FR-07.6 | The **Predictive Ecological Impact** badge shall show if a destination is forecast to face ecological degradation (sourced from `ecological_risk_tier` in recommendation response) |

---

## FR-08 · Pricing Fairness & Hidden Cost Detection (Patent Feature)

| ID | Requirement |
|---|---|
| FR-08.1 | The hotel detail screen shall call `POST /api/v1/pricing/analyze` and display a **Price Fairness Score** (0–100) |
| FR-08.2 | If the backend detects hidden costs (fees above threshold), the app shall show a ⚠️ **"Hidden fees detected"** banner with the flagged amounts listed |
| FR-08.3 | If the pricing guardrail `PRICE_DISCRIMINATION` is triggered (>15% above base), the app shall display: "This price may be above fair market rate" |
| FR-08.4 | A price trend indicator (RISING / STABLE / FALLING) shall be shown on the booking detail screen |

---

## FR-09 · Hotel Listing & Booking Flow

| ID | Requirement |
|---|---|
| FR-09.1 | Hotel cards shall show: image carousel, name, star rating, price/night, eco score, safety flag of the destination, eco badge |
| FR-09.2 | Hotel list shall support filters: price range, star rating, eco-certified, free cancellation |
| FR-09.3 | Hotel detail page shall show: full amenities, room types, pricing breakdown (from `/pricing/analyze`), eco detail, safety context |
| FR-09.4 | Date picker for check-in / check-out shall use Android `MaterialDatePicker` |
| FR-09.5 | "Add to Cart" shall add the hotel booking with a price snapshot to Room DB `cart_items` table |

---

## FR-10 · Real-Time Safety Alerts via WebSocket Mesh (Patent Feature)

| ID | Requirement |
|---|---|
| FR-10.1 | On app launch (post-login), the app shall establish a **WebSocket connection** to `wss://{backend}/ws/mesh/sync?node_id={device_id}` using OkHttp WebSocket client |
| FR-10.2 | The app shall listen for `FLAG_CHANGE_ALERT` messages from the gossip mesh — sent when any saved destination's safety flag changes (GREEN→YELLOW, YELLOW→RED, etc.) |
| FR-10.3 | On receiving a `FLAG_CHANGE_ALERT`, the app shall immediately update the destination card's flag color in the UI without requiring a refresh |
| FR-10.4 | A **push notification** shall be fired via FCM when the app is backgrounded and a saved destination's flag changes |
| FR-10.5 | Each mesh alert shall include an HMAC-SHA256 signature. The app shall verify the signature before processing the alert to prevent spoofed messages |
| FR-10.6 | The WebSocket connection shall auto-reconnect with exponential backoff on network interruptions |

---

## FR-11 · Cart & Order Placement

| ID | Requirement |
|---|---|
| FR-11.1 | Cart shall show all added bookings with price breakdown (subtotal, taxes, total) |
| FR-11.2 | Users shall be able to remove items and edit guest count |
| FR-11.3 | Place Order shall call `POST /api/v1/booking/orders` — response includes `booking_id` and `blockchain_ref` for the transaction |
| FR-11.4 | Order confirmation screen shall show booking ID, safety flag at time of booking, eco impact summary |
| FR-11.5 | Past orders shall be accessible in Profile → "My Bookings" |

---

## FR-12 · Trip Planner

| ID | Requirement |
|---|---|
| FR-12.1 | Users shall create named trips with start/end dates |
| FR-12.2 | Destinations and hotels can be added from their detail pages |
| FR-12.3 | Day-by-day itinerary view with drag-to-reorder |
| FR-12.4 | Trips stored in Room DB; optionally synced to Firebase Firestore |

---

## FR-13 · Offline Support

| ID | Requirement |
|---|---|
| FR-13.1 | Previously loaded destinations and hotels shall be accessible offline from Room DB cache |
| FR-13.2 | Safety flag history and blockchain refs shall be cached in Room DB |
| FR-13.3 | The app shall show a clear **"Offline Mode — Cached Data"** banner when network is unavailable |

---

*End of Functional Requirements — STS-FR-001 v2.0*
